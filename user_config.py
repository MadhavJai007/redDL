"""
This file will be used to store config settings that will allow user to personalize their experience according to their needs
"""

# Just to be safe, ensure that the root folder is a directory that does not require admin privileges to access by default
# or you could just run redDL in admin mode
# Make sure to use the "r" prefix before putting the directory path in quotes. Otherwise part of your directory might become unreadable
ROOT_DOWNLOADS_FOLDER = r"D:\redDL downloads"
